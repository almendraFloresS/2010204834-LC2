@model IEnumerable<_2010204834_ENT.Entities.Bus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.placa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nroAsientos)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.placa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nroAsientos)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBus }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBus }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBus })
        </td>
    </tr>
}

</table>
